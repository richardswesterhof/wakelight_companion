package com.richardswesterhof.wakelightcompanion.settings_page.custom_prefences

import android.content.Context
import androidx.preference.DialogPreference
import androidx.preference.PreferenceViewHolder


class DevicePreference(context: Context?) : DialogPreference(context) {
    override fun onBindViewHolder(holder: PreferenceViewHolder?) {
        // TODO: just calls super implementation for now, but this is where some custom styling should go??
        super.onBindViewHolder(holder)
    }


}